set -e

# 이 프로젝트엔 아직 자동화된 테스트 스위트가 없다(ISSUES.md/RISKS.md 참고 — 지금까지는
# 기능을 바꿀 때마다 Playwright로 수동/일회성 검증만 해왔음). 최소한의 안전망으로 모든 JS
# 파일의 구문 오류만이라도 push 전에 걸러낸다.
echo "==> 전체 구문 체크 (src/, public/)"
find src public -name '*.js' -print0 | xargs -0 -n1 node --check

echo "==> 포맷 확인 (전체)"
npx prettier --check .
