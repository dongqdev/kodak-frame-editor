# DECISIONS

왜 이렇게 만들었는지, 검토했던 대안, 재검토 조건을 기록한다. 나중에 같은 논쟁을 반복하지
않기 위함 (`/app/DEVELOPMENT.md` 3종 문서 컨벤션).

## 접근 제어: 인증 없음, 링크 공유

**결정**: 로그인 없음. 배포 URL을 아는 사람 누구나 사용 가능. 오남용
방지용 rate limit도 없음.

**왜**: 가족끼리만 링크를 공유하는 용도라 로그인 UX를 넣을 만큼의 위험이 없다고 판단
(2026-08-16 그릴링 세션에서 명시적으로 확인).

**재검토 조건**: 링크가 가족 범위를 벗어나 퍼지거나, 실제로 원치 않는 사진이 액자에 올라오는
사고가 발생하면 최소한의 접속 제한(공유 비밀번호, rate limit)을 추가한다.

## 크롭 방식: 비율만 맞추고 해상도는 원본 유지

**결정**: 1000×1600으로 강제 리사이즈하지 않는다. 크롭은 비율만 1000:1600(또는 2단 배치 시
그 절반)에 맞추고, 픽셀 해상도는 원본 그대로 유지한 채 내보낸다.

**왜**: 애초에 겪은 문제가 "비율이 안 맞아서 액자가 이상하게 보여준다"였지, 해상도 자체가
문제였던 게 아니다. 액자 쪽이 최종 스케일링은 알아서 처리하는 걸로 실사용 확인됨. 비율만
맞으면 액자가 나머지를 처리하게 두는 게 화질 손실이 가장 적다.

**재검토 조건**: 액자가 실제로는 스케일링을 잘 못한다는 게 나중에 확인되면, 정확히 1000×1600
픽셀로 강제 리사이즈하는 옵션을 추가로 고려.

## 발신 이메일 계정: 기존 공유 자격증명 재사용

**결정**: 이미 다른 자동화(blog-bot 알림)에 쓰고 있던, 개인 메일과 분리된 발신 전용 Gmail
계정을 그대로 재사용. 새 전용 계정을 만들지 않음.

**왜**: 액자 이메일 게이트웨이는 발신자 제한이 없다고 실사용으로 확인됨. 이미 개인 계정과
분리된 계정이 있는데 굳이 하나 더 만들 이유가 없음.

## 배치 발송: 용량 + 장수, 둘 중 먼저 걸리는 기준으로 분할

**결정(2026-08-16 개정)**: 여러 장을 한 번에 보낼 때, 이메일 첨부 총 용량이 임계값
(`MAIL_BATCH_MAX_BYTES`, 기본 20MB)을 넘거나 장수가 임계값(`MAIL_BATCH_MAX_COUNT`, 기본
9장)을 넘으면 — 둘 중 먼저 도달하는 조건에서 — 자동으로 여러 통으로 나눠 보낸다. 사용자가
직접 계산할 필요 없음.

**왜(원래는 용량 기준만 있었음)**: 처음엔 "사진마다 해상도가 달라서 장수 기준은 무의미하다"고
판단해 용량 기준만 뒀다. 그런데 사용자가 코닥 액자 관련 자료를 직접 조사해서 "이메일 한
통에 10장 이상 넣으면 일부 사진이 누락되거나 처리가 지연될 수 있다"는 안내를 찾아 공유해줬다
(조사 도구의 요약이라 코닥 공식 1차 문서로 확인된 건 아니지만, 실제 기기 시리얼 번호 기준
조사였고 방향이 합리적이라 반영함). 용량만 기준으로 두면 사진이 작을 때(예: 1MB짜리 30장)
한 통에 20장 넘게 들어갈 수 있어 이 제한을 놓칠 수 있었다 — 장수 상한을 추가로 걸어서
용량이 넉넉해도 9장을 넘기지 않게 함.

**재검토 조건**: "9장 제한"이 실제로 이 기기에 맞는지 코닥 공식 문서나 실사용으로 아직
확정 검증은 안 됨([[RISKS]] 참고) — 실사용 중 9장 이하로도 문제가 생기거나, 반대로 9장
넘게 보내도 전혀 문제없다는 게 확인되면 값을 조정할 것.

## R2 저장: 재해복구 백업 전용, 히스토리 기능 아님

**결정**: 편집(크롭) 결과물만 `kodak/<KST타임스탬프>_<파일명>` 형태로 R2에 평평하게(날짜
폴더 없이) 영구 보관. 원본은 저장하지 않음. 화면에 노출되는 히스토리/조회 UI는 없음.

**왜**: 목적이 "액자가 오류로 초기화됐을 때 사람이 R2 콘솔에서 직접 골라 재수동업로드"하는
것뿐이라, 날짜별 폴더로 나누면 오히려 한 번에 훑어보기 불편함. 파일명 맨 앞 타임스탬프로
시간순 정렬만 되면 충분. 재업로드 자체는 자동화하지 않기로 함 — 자동화 비용 대비 얻는 게
적음(액자 오류는 드문 이벤트).

**검토했던 대안**: 날짜별 폴더 + 원본도 같이 저장 — 저장 용량이 더 빨리 참, 폴더 탐색이
오히려 느려서 기각.

**재검토 조건**: 재해복구 상황이 실제로 여러 번 발생하고 "그때 그 사진 찾기"가 매번
번거로우면, 최소한의 조회 UI(예: 날짜 검색)를 추가.

## 배경 채우기 + "나란히 2장 배치" (인생네컷 대응)

**결정**: 사진 확대/축소 하한을 없애고(`viewMode:0`), 프레임을 다 못 채우는 빈 자리는
사용자가 고른 배경색(가장자리 자동 추출 후보 4개 + 자유 색상피커)으로 채운다. 추가로
"나란히 2장 배치" 체크박스를 켜면 오른쪽에 **좌우 반전**된 동일 사진을 배치해서,
인생네컷처럼 1000:1600보다 훨씬 좁고 긴 사진도 잘리지 않고 다 담기게 한다.

> **2026-08-16 재설계**: "나란히 2장 배치"의 내부 방식이 바뀌었다. 아래 "거울모드는 크롭
> 없이 통째로" 항목 참고 — 예전엔 절반 폭으로 **잘라서** 붙였는데, 지금은 **자르지 않고**
> 슬롯 안에 통째로 넣는다.

**왜**: 처음엔 "인생네컷 전용" 기능으로 좁게 설계하려 했으나, 축소+배경색이라는 범용
메커니즘으로 만들면 반대 케이스(가로로 넓은 파노라마)까지 별도 설계 없이 자연히 커버됨.

**스포이드는 처음부터 생략**: 가장자리 4곳 자동 추출 후보가 대부분의 경우 스포이드의 상위
호환(배경과 맞닿는 지점에서 뽑으니까)이라 필요성이 낮다고 판단. 필요해지면 나중에 추가
([[RISKS]] 참고).

## 확대/축소 모드: 자유 / 높이 맞춤 / 너비 맞춤 (라디오 3종)

**결정**: 기존에 있던 "즉시실행 버튼" 방식(너비 맞추기 버튼 한 번 누르면 끝)을 폐기하고,
라디오 3개로 재설계:

- **자유**: 기존 동작 그대로(무제한 축소, 배경 채우기용 프레임 이탈 등 전부 유지)
- **높이 맞춤**: 높이가 항상 프레임에 고정, 좌우로만 드래그, 확대·축소 잠금, 프레임 밖으로
  안 나가게 경계에서 clamp
- **너비 맞춤**: 대칭(상하만 드래그)

**왜**: 실사용 피드백 — "왼쪽 기준으로 딱 맞추고 싶은 사진들이 있다"는 요구는 즉시실행 버튼
방식으로는 채울 수 없었음(누른 다음 다시 자유롭게 드래그가 풀려버림). 특정 축을 계속
고정해두는 "모드"가 필요했음.

**잠금 모드에서 확대·축소를 막은 이유**: 배율이 바뀌면 "높이/너비 딱 맞음" 자체가 깨져서
경계 clamp 로직이 무의미해짐.

**구현 메모(재발 방지용)**: [[ISSUES]]의 "나란히 2장 배치 반복 토글 시 프레임 축소" 항목
참고 — `cropper.setAspectRatio()`를 부르기 전엔 항상 캔버스를 커버 상태로 채워둬야 한다.

## 배포/인프라

개인 서버 공통 컨벤션 그대로: 개발 서버에서 작업 → 운영 서버로 `git pull` 배포, 포트 8003.
R2는 다른 앱과 버킷을 공유하고 프리픽스만 `kodak/`로 분리한다(다른 앱들의 `<앱이름>/...`
관례와 다르게 짧게 간 이유는 위 "R2 저장" 결정 참고). 외부 공개는 서브도메인 A레코드 +
Nginx Proxy Manager 프록시 등록으로 붙였다.

> 이 절은 원래 개인 인프라의 구체적인 호스트/도메인/계정을 적어뒀는데, 저장소를 공개로
> 전환하면서 일반화했다(2026-08-17). 실제 값은 저장소 밖 `/app/DEVELOPMENT.md`와 `.env`에만 둔다.

## Git 훅: sap-harness가 아니라 `/app/DEVELOPMENT.md`의 가벼운 버전으로

**결정**: `/app/DEVELOPMENT.md`가 새로 정한 "새 프로젝트는 husky 필수" 컨벤션을 뒤늦게
적용하되, 기존 참고 프로젝트인 sap-harness의 pre-push 훅(변경 파일 종류별로 검사 범위를
나누는 정교한 로직)은 그대로 베끼지 않고, 가이드 문서가 명시한 최소 스펙만 구현:

- pre-commit: `.env` 스테이징/시크릿 패턴 차단 → lint-staged(prettier) → 스테이징 파일
  구문 체크
- pre-push: 전체 구문 체크 + `prettier --check` (진짜 테스트 스위트는 아직 없음, [[RISKS]]
  참고)

**왜**: sap-harness는 워크스페이스 여러 개, TypeScript 빌드, vitest 등 이 프로젝트에는 없는
것들을 전제로 짜여 있다. 그 복잡도를 그대로 옮기면 "무겁게 짜면 커밋마다 느려져서 습관이
깨진다"는 가이드 문서 자신의 원칙과 충돌한다.

**겪은 문제와 조치**: `"prepare": "husky"`를 그대로 쓰면 운영 서버의 `npm install
--omit=dev`가 `husky` 명령을 못 찾아 배포 자체가 실패했다([[ISSUES]] 버그 #6). `"husky ||
true"`로 완화해서 고침 — 새 프로젝트에 이 패턴을 복사할 때 반드시 같이 가져갈 것.

## 편집은 다운스케일 미리보기로, 전송 시점에만 원본 해상도 재구성

**결정**: 편집 화면에서 Cropper에 물리는 이미지는 항상 `PREVIEW_MAX_DIM`(1280px) 이하로
축소한 버전이다. 원본은 인코딩된 상태로만 가볍게 들고 있다가(`entry.fullResBlob`), "전송"
누르는 시점에 사진을 하나씩 순서대로 다시 디코드해서 그때 크롭을 적용한다. 크롭 위치/배율은
"이미지 대비 비율"로 저장해뒀다가 원본 해상도에 그대로 곱해서 재현한다(해상도가 달라도
동일한 결과가 나옴).

**왜**: 사진 24장을 한꺼번에 편집하면 탭이 크래시하는 실사용 버그([[ISSUES]] 버그 #7)의
진짜 원인이, 편집 화면에 동시에 떠 있는 카드 전부가 원본 해상도 이미지를 계속 메모리에 물고
있는 것이었다. "동시 처리를 순차 처리로" 바꾸는 정도로는 못 고쳤다 — 로딩이 다 끝난 뒤에도
메모리 사용량은 그대로였기 때문. 편집용과 최종 결과물용 이미지를 분리하는 것 말고는 근본
해결이 없었다.

**검토했던 대안**: 서버로 원본을 올려서 서버가 병렬로 합성하는 방안도 검토했지만
([[RISKS]] 참고했던 초안), 새 업로드 엔드포인트/임시 저장/리뷰 흐름 재설계가 필요한 훨씬
큰 변경이라 기각. 지금 방식은 서버·업로드·리뷰 구조를 하나도 안 바꾸고 브라우저 안에서만
해결한 절충안.

**부수 효과**: Cropper.js를 다시 쓰지 않고 순수 `canvas.drawImage`로 원본을 크롭하게 되면서,
EXIF 회전 버그([[ISSUES]] 버그 #5)의 우회 코드(`effectiveNaturalSize`)가 전송 단계에서는
아예 필요 없어졌다 — `<img>`+`drawImage`는 브라우저가 이미 방향 보정한 픽셀을 그대로 그려주기
때문. 편집용 저해상도 크로퍼 쪽에서는 여전히 그 헬퍼를 쓰지만, 다운스케일 자체가 이미
방향 보정된 픽셀이라 사실상 항상 rotate=0으로 no-op이다.

**부수 버그**: 이 작업 도중, 자유 모드에서 사진을 극단적으로 축소해두면(배경이 대부분)
"비율 재현" 계산 결과 원본보다 훨씬 큰 크롭 영역이 나와 결과물이 비정상적으로 거대해지는
걸 발견했다(4574×7312 실측). `MAX_OUTPUT_DIM`(6000px) 캡으로 막음 — 자세한 내용은
[[ISSUES]] 버그 #7.

**재검토 조건**: 실제 폰으로 30장 꽉 채운 고해상도 배치를 테스트해서 문제 없는지 확인 필요
([[RISKS]] 참고). `MAX_OUTPUT_DIM` 캡 때문에 극단적으로 축소한 사진의 화질 컴플레인이
오면 캡 값을 재조정.

**실사용 검증 결과(2026-08-16)**: 실제 폰으로 30장(다양한 인생네컷/일반 사진 혼합)을
편집→전송까지 완료. 다운스케일 아키텍처 자체는 문제 없이 동작함을 확인 — 다만 이 테스트
과정에서 별개의 문제(아래 "응답은 업로드 접수 즉시" 결정, [[ISSUES]] 버그 #8)를 발견함.

## 응답은 업로드 접수 즉시, 실제 발송은 백그라운드에서

**결정**: `/api/send`는 사진을 받자마자(R2 백업/이메일 발송을 시작하기 _전에_)
`{ok:true, accepted:N}`으로 즉시 응답한다. R2 업로드와 이메일 발송(사진이 많으면 여러 통)은
응답 이후 백그라운드에서 계속 진행되고, 실패해도 서버 로그에만 남긴다 — 클라이언트에
알릴 방법이 없다.

**왜**: 실사용 테스트에서 30장(이메일 4통) 전송 시 화면에 "전송 실패(HTTP 504)"가 떴지만,
Gmail 보낸메일함을 확인하니 4통 다 정상 발송되어 있었다 — 실제로는 완전히 성공했는데
Nginx Proxy Manager의 응답 대기 시간(기본 60초 안팎)을 우리 서버의 전체 처리 시간(R2
업로드 + 순차 이메일 발송)이 넘겨버려서 생긴 거짓 실패였다([[ISSUES]] 버그 #8).
"몇 분 걸리더라도 접수만 되면 끝까지 처리해서 사용자에게 알리지 않아도 된다"는 이 앱의
성격(액자에 이메일로 올라가는 것만 확인되면 충분, 실시간 결과 확인이 필요 없음)과도 잘
맞는 방향.

**검토했던 대안**: Nginx Proxy Manager의 `proxy_read_timeout`을 늘리는 방안도 있지만,
그건 NPM 대시보드 설정이라 내가(에이전트가) 직접 못 바꾸고 사용자가 수동으로 해야 한다
([[DEVELOPMENT.md]] 참고 — NPM/DNS는 애초에 사용자가 직접 등록했음). 근본적으로는 "얼마나
걸리든 타임아웃 걱정 없이" 만드는 게 이 방식만 못하다 — 타임아웃 값을 아무리 늘려도 사진
수가 더 늘면 다시 재발할 수 있는 반면, 응답을 앞당기는 방식은 처리 시간과 완전히 무관해짐.

**트레이드오프**: R2/이메일이 진짜로 실패해도 사용자는 알 수 없다. 재검토 조건은 아래
[[RISKS]] 참고.

## 거울모드는 크롭 없이 통째로 (2026-08-16 재설계)

**결정**: "나란히 2장 배치"에서 사진을 **자르지 않는다**. 전체(1000×1600)를 좌우 반반(각
500×1600)으로 나누고, 각 슬롯에 사진을 비율 그대로 통째로 넣는다(contain). 좌우 4%(500 중
20px) 여백을 두고 가운데 정렬하며, 남는 자리는 배경색으로 채운다. 인생네컷 자동감지 시
배경색을 사진 가장자리 색으로 자동 지정한다.

크롭이 없으므로 거울모드 카드에는 크로퍼를 아예 만들지 않고, 합성 결과를 보여주는 미리보기
캔버스로 대체한다. 맞춤 라디오와 재설정 버튼도 숨긴다.

**왜**: 예전 방식은 전체 폭의 46%짜리 아주 좁은 비율(0.2875)로 **잘라서** 붙였다. 문제가
두 가지였다.

1. **사진이 잘렸다**. 인생네컷 원본 비율(약 0.336)이 0.2875보다 넓어서, "높이 맞춤"으로
   쓰면 좌우가 14.6% 잘려나갔다. 애초에 "잘리지 않게 하려고" 만든 기능인데 잘리고 있었음.
2. **잘리지 않게 하려면 위아래 배경을 두는 "너비 맞춤"을 써야 했는데**, 그러면 크롭 영역이
   이미지 밖으로 나가면서 iOS Safari에서 세로로 늘어나는 버그를 밟았다([[ISSUES]] 버그 #9).

즉 두 선택지가 "잘리거나 / 늘어나거나"였다. 자르지 않는 방식으로 바꾸면 둘 다 사라진다 —
비율이 바뀔 일도, 사진이 잘릴 일도, 크롭 영역이 이미지 밖으로 나갈 일도 없다.

**여백 4%를 고른 이유**: 슬롯 500 중 좌우 20px씩. 가운데는 두 슬롯의 여백이 만나 40px
거터가 되고 바깥쪽은 20px이 된다 — 별도의 거터/바깥여백 상수 없이 하나의 값으로 자연스러운
간격이 나온다.

**한계**: 거울모드에서는 사진을 부분 확대해서 특정 부분만 보여주는 게 불가능해졌다(크롭
자체가 없으므로). 인생네컷은 스트립 전체를 보여주는 게 목적이라 문제되지 않는다고 판단했고,
부분만 쓰고 싶으면 거울모드를 끄고 일반 크롭을 쓰면 된다. 요청이 오면 재검토.

## UI: 따뜻한 다크 테마 + 디자인 토큰 (2026-08-16)

**결정**: `ui-ux-pro-max` 스킬 조회 결과를 반영해 전면 재정비. 색/간격/모서리/그림자/모션을
CSS 변수(토큰)로만 쓰고, 다크를 기본으로 하되 `prefers-color-scheme: light`면 자동 전환한다.

**왜 다크 기본**: 사진이 주인공인 도구라 화면 크롬이 어두워야 사진이 도드라진다(스킬 DB의
"Photo Editor & Filters" 팔레트도 다크였음).

**왜 앰버 강조색**: 스킬 DB가 준 팔레트의 강조색은 차가운 바이올렛/시안이었는데, "따뜻하고
친근한 가족용"이라는 요구와 안 맞았다. 팔레트의 **다크 표면/대비 구조는 그대로 쓰되 강조색
계열만 코닥을 연상시키는 앰버로 교체**했다. 라이트 모드에서는 흰 글자와 4.5:1을 확보하려고
더 진한 앰버(#B35A09)를 쓴다.

**접근성 기준선**(회귀 시 이 값들을 다시 확인할 것):

- 색 대비 전 항목 WCAG AA 4.5:1 통과 — 다크 6.73~~16.73:1, 라이트 4.79~~16.64:1
- 모든 조작 요소 터치 히트영역 44px 이상. 체크박스/라디오는 라벨로 감싸서 확보하고,
  배경색 스와치는 `::after`로 히트영역만 넓힌다(보이는 크기는 28px 유지)
- viewport에서 `maximum-scale` 제거 — 확대를 막고 있었고, 이건 저시력 사용자를 차단한다
- 포커스 링 상시 표시(2px + 오프셋), `prefers-reduced-motion` 존중
- 아이콘은 문자(＋/✕) 대신 인라인 SVG, 장식용은 `aria-hidden`

## 결과물을 액자 해상도(긴 변 1920)로 미리 축소 (2026-08-17)

**결정**: 최종 결과물의 긴 변을 `FRAME_TARGET_LONG_DIM`(1920)으로 브라우저에서 미리 축소해서
보낸다. 위 "크롭 방식: 비율만 맞추고 해상도는 원본 유지" 결정을 **뒤집은 것**이다 — 그 항목에
적어둔 재검토 조건("액자가 실제로는 스케일링을 잘 못한다고 확인되면")이 실제로 걸렸다.

**왜**: 30장 실사용에서 인생네컷만 유독 액자에서 흐리다는 피드백. 파일 자체는 선명했다.
거울모드 결과물은 한 장에 컷이 8개(4컷 × 좌우 2벌) 들어가 3592×5746(20MP)까지 커지는데,
액자(패널 1920×1200 또는 1280×800)가 이걸 1/3로 줄이면서 내장 스케일러 품질이 그대로
드러난 것으로 판단. 축소를 액자에 맡기지 않고 우리가 끝내면 액자는 손댈 게 없어진다.
1920이면 두 패널 모델을 다 커버한다.

**부수 효과(큼)**: 첨부 용량이 급감한다(테스트에서 3.5MB → 0.1MB, 실사진도 수 배 감소).
20MB/9장 배치 분할이 거의 안 걸리게 되므로 발송이 단순해지고 빨라진다.

**단계적 축소를 쓰는 이유**: 절반씩 줄여 목표의 2배 안에서 마무리한다(mipmap 방식).
단, **Chrome에서는 단일 단계 축소와 품질 차이가 측정되지 않았다**(3배 축소 후 디테일 지표
88.6 vs 88.6) — `imageSmoothingQuality:'high'`가 이미 제대로 샘플링하기 때문. 그럼에도
단계적으로 두는 건 실사용 환경이 iOS Safari이고 이 엔진이 Chrome과 다르게 동작한 전례가
있어서다([[ISSUES]] 버그 #9).

**한계/재검토 조건**: 이걸로도 인생네컷이 여전히 흐리면 그건 액자 패널 해상도의 물리적 한계다
(한 장에 컷 8개를 넣는 이상 개별 컷은 패널의 1/8 이하 면적을 쓸 수밖에 없음). 그 경우 4컷을
낱장으로 쪼개 보내는 방식을 고려한다. 또, 나중에 더 고해상도 액자를 쓰게 되면 1920 값을 올릴 것.

## 액자 번호만 입력받아 공개 (2026-08-17)

**결정**: 받는 주소를 통째로 입력받지 않고 **`@` 앞 숫자(액자 번호)만** 입력받는다. 도메인
`mx1.kodakframes.com`은 서버 상수로 박아두고 서버가 붙인다. 이로써 가족 전용이던 앱을 같은
액자를 쓰는 누구나 쓸 수 있게 열었다(위 "접근 제어: 인증 없음" 결정의 연장).

**왜 도메인을 서버에 박는가 (핵심)**: 로그인이 없는 앱에서 받는 주소 전체를 입력받게 하면
**아무 주소로나 이미지를 보내주는 공개 스팸 발송기**가 된다. 그 발송이 우리 Gmail 계정으로
나가므로 악용 시 계정이 정지되고, 같은 계정을 쓰는 다른 앱(blog-bot 알림 등)까지 같이 죽는다.
숫자만 받으면 코닥 액자 외의 주소로는 **애초에 보낼 방법이 없다** — 검증으로 막는 게 아니라
구조적으로 불가능하게 만든 것이라 훨씬 안전하다. (원래는 "@mx1.kodakframes.com으로 끝나는지
검증"하는 안을 검토했으나, 사용자가 "뒤 주소는 같으니 앞 숫자만 받으면 되지 않나"라고 지적해서
더 단순하고 안전한 지금 방식으로 바뀜.)

**검증**: `/^\d{4,16}$/`만 통과. 주소 통째로 주입, 콤마로 수신자 추가, 개행 주입 등 10개
케이스 단위 테스트로 전부 거부 확인.

**발송량 제한**: IP당 하루 `RATE_LIMIT_PHOTOS_PER_DAY`(기본 300장). 스팸 발송기 위험은
도메인 고정으로 사라졌지만, 한 사람이 Gmail 계정 발송 한도를 다 태우면 다른 사람이 못 쓰게
되므로 최소한의 상한을 둔다. 서버 메모리 기반이라 재시작하면 초기화되는데, 이 규모에서는
Redis 같은 걸 붙일 이유가 없다고 판단.

**남은 한계**: 액자 번호는 검증할 방법이 없다(오타를 내면 남의 액자로 갈 수 있음 — 다만
번호를 정확히 맞춰야 하므로 실수로 그럴 확률은 낮다). 발송 로그에 수신 주소를 남겨둬서
문제가 생기면 추적은 가능하게 했다.
