# ISSUES

겪은 버그와 조치. 새 기능을 추가하거나 크롭/줌 관련 코드를 건드릴 때는 아래 회귀
체크리스트부터 훑어볼 것.

## 회귀 체크리스트 (크롭/줌 관련 변경 시 반드시 확인)

- [ ] 사진 업로드 후 "미리보기로 이동" → "편집으로 돌아가기"를 왕복해도 카드가 정상적으로
      보이는가(빈 배경색만 남지 않는가)?
- [ ] "나란히 2장 배치" 체크박스를 5회 이상 빠르게 토글해도 정상 동작하는가(켜면 크로퍼가
      사라지고 합성 미리보기, 끄면 크로퍼 복귀)?
- [ ] **결과물에 비균등 왜곡(가로/세로 배율이 다름)이 없는가?** 특히 거울모드. 판정법:
      정확한 정원이 그려진 테스트 이미지를 넣고 결과물에서 원의 bbox가 정사각형인지 본다
      (버그 #9 참고 — 데스크톱 Chrome에서는 멀쩡한데 iOS Safari에서만 늘어나던 사례가
      있으므로, `drawImage`에 넘기는 소스 사각형이 이미지 밖으로 나가지 않는지도 같이 확인).
- [ ] 높이 맞춤/너비 맞춤 모드에서, 사진을 자유 모드로 많이 축소했다가 다시 잠금 모드로
      전환해도 정상적으로 프레임 중앙에 재배치되는가?
- [ ] 아이폰으로 찍은 세로 사진(EXIF 회전 태그 있음)을 업로드했을 때 커버 핏/높이 맞춤/너비
      맞춤이 사진 전체를 제대로 담는가(과도하게 확대되어 잘려나가지 않는가)? 테스트용 EXIF
      이미지 만드는 법은 이 문서 하단 참고.
- [ ] 자유 모드에서 휠/핀치로 무제한 축소가 여전히 되는가(배경 채우기 기능의 전제)?
- [ ] "나란히 2장 배치" + 배경색 커스텀 조합에서 실제 전송 결과물의 거터/여백에 배경색이
      제대로 채워지는가?
- [ ] `package.json`의 `devDependencies`나 `scripts.prepare`를 건드렸다면, `rm -rf node_modules && npm install --omit=dev`로 운영 서버(140)와 동일한 설치가 여전히 성공하는가? (버그 #6 참고 — 로컬 dev 설치만 확인하고 넘어가면 이 클래스의 실패를 못 잡음)
- [ ] 사진 여러 장(가능하면 20장 이상, 고해상도)을 한꺼번에 선택해도 편집 화면에 전부 카드가
      생기고 초기 업로드 화면으로 되돌아가거나 탭이 죽지 않는가? (버그 #7 참고 — 특히
      모바일에서, 특히 HEIC로. 되도록 실제 폰으로 확인할 것)
- [ ] 큰 원본(예: 4000px 이상)을 업로드했을 때 편집 화면의 미리보기는 작아 보여도, 실제
      전송 결과물은 원본 해상도를 유지하는가? (다운스케일 미리보기 아키텍처 회귀 확인용)
- [ ] 자유 모드에서 사진을 아주 많이 축소해서 배경이 대부분인 상태로 전송해도 결과물 크기가
      비정상적으로 거대해지지 않는가(`MAX_OUTPUT_DIM` 캡, 버그 #7 하단 참고)?
- [ ] 사진을 많이(20장 이상) 실제로 전송했을 때 "액자로 전송" 버튼을 누르면 몇 초 안에
      응답이 오는가(오래 기다리게 하면 버그 #8이 재발할 조건을 만드는 것 — `/api/send`는
      항상 업로드 접수 즉시 응답해야 하고, R2/이메일은 그 뒤 백그라운드에서 진행되어야 함)?
- [ ] 30장이 이미 선택된 상태에서 "+ 사진 더 추가"가 비활성화되고 안내 문구가 보이는가?
      한 장을 지우면 다시 활성화되는가?
- [ ] 용량은 작지만 장수가 많은 사진(예: 15장 이상의 작은 사진)을 보내도 이메일이 9장씩
      나뉘어 발송되는가(용량만으로는 안 걸리는 경우도 장수로 걸려야 함, `MAIL_BATCH_MAX_COUNT`
      참고)? 실사용 테스트 시 **액자 화면에서 실제로 몇 장이 떴는지 직접 세어서 확인**할 것
      — 지금까지는 "이메일 발송 성공"까지만 확인했지 액자 반영 여부는 확인한 적 없음.

## 해결된 버그

### 1. 리뷰 화면에서 편집으로 돌아가면 카드가 완전히 빈 배경색으로만 보임

**증상**: 여러 장 편집 → 미리보기 → "편집으로 돌아가기" 하면 크롭 카드가 통째로 단색
배경만 남고 사진이 사라짐.
**원인 추정**: 리뷰 화면 체류 중 편집 카드가 `display:none`으로 숨겨지는데, 그 사이 모바일
브라우저의 뷰포트 변화(주소창 접힘/펼침 등)로 리사이즈 이벤트가 발생하면 Cropper.js의
`responsive` 내부 처리가 0 크기 컨테이너를 기준으로 캔버스 상태를 깨뜨림.
**조치**: Cropper 자체의 `responsive` 옵션을 끄고, "편집으로 돌아가기"/"이 사진 편집하기"
시 크롭 위치(`getCanvasData`/`setCanvasData`)를 보존한 채 Cropper 인스턴스를 통째로
재생성하는 방식으로 전환. `getData()`/`setData()`(이미지 자연좌표 기준)는 배경 채우기용
"프레임 밖으로 나간" 상태를 0으로 클램핑해버려서 못 씀 — 반드시 캔버스 데이터 방식 사용.

### 2. "나란히 2장 배치" 여백이 거의 안 보임

**증상**: 2단 배치 결과물에서 두 사진 사이 거터와 바깥 테두리 여백이 육안으로 거의 안 보임
(2.4% 거터만 있었음).
**조치**: 바깥 여백 2% + 거터 4%로 늘림. `OUTER_FRACTION`/`GUTTER_FRACTION` 상수 참고
(`public/app.js`).

### 3. 축소한 채로 크롭 박스 밖까지 드래그해두면 "너비/높이에 맞추기"가 안 먹힘

**증상**: 사진을 많이 축소해서 프레임 밖으로 완전히 벗어난 상태에서 맞춤 버튼을 눌러도
아무 변화가 없어 보임. 재설정을 먼저 해야만 동작.
**원인**: `zoomTo()`는 "현재 캔버스 위치"를 기준으로 크기만 바꾼다. 이미지가 이미 크롭 박스
밖에 있으면 크기를 맞춰도 위치가 그대로라 여전히 박스 밖에 남음.
**조치**: 크기 계산과 동시에 크롭 박스 정중앙으로 명시적 재배치(`setCanvasData`)하는
`fitCanvas()`로 통일. 커버 핏/너비 맞춤/높이 맞춤 전부 이 함수 공유.

### 4. "나란히 2장 배치"를 반복 토글하면 프레임이 점점 작아짐

**증상**: 특히 높이/너비 맞춤 잠금 모드에서 체크박스를 여러 번 토글하면 크롭 프레임이
매번 조금씩 줄어들다가 결국 손톱만해짐.
**원인**: `cropper.setAspectRatio()`는 내부적으로 "현재 캔버스(이미지) 크기"를 새 크롭
박스의 상한으로 사용한다. 잠금 모드에서는 캔버스가 크롭 박스보다 작게(한쪽 축에 배경이
드러나게) 되어 있는 게 정상 상태인데, 그 상태로 `setAspectRatio()`를 부르면 그 작아진
캔버스를 기준으로 크롭 박스 자체가 영구적으로 줄어들어버림. (처음엔 우리 쪽 'crop' 이벤트
클램프 핸들러가 겹쳐서 도는 게 원인인 줄 알았으나, 핸들러를 완전히 떼고 재현해봐도 동일하게
발생해 Cropper.js 자체의 동작임을 확인함 — 진단 과정에서 헤맨 기록.)
**조치**: `applyAspect()`에서 `setAspectRatio()`를 부르기 전에 항상 `fitCanvas(cropper,
'cover')`로 캔버스를 먼저 꽉 채워서 상한 문제 자체를 없앰.

### 5. 특정 사진만 높이/너비 맞춤이 안 됨 (EXIF 회전)

**증상**: 대부분의 사진은 정상인데 특정 사진만 "높이 맞춤"을 눌러도 이상하게 과도하게
확대되어 사진의 상당 부분이 잘려나감.
**원인**: 스마트폰 세로 사진은 실제 픽셀이 가로로 저장되고 EXIF `Orientation` 태그로만
회전 정보를 표시하는 경우가 대부분이다(카메라를 정상적으로 들고 세로로 찍으면 발생).
Cropper.js는 `imageData.naturalWidth/naturalHeight`를 "회전 전" 원본 기준으로 주고
`rotate` 필드로 회전값을 따로 알려주는데, 우리 `fitCanvas`가 이를 무시하고
naturalWidth/naturalHeight를 화면상 가로/세로로 오해해서 비율을 계산했음.
**조치**: `effectiveNaturalSize()` 헬퍼 추가 — `imageData.rotate`가 90/270도면 가로/세로를
바꿔서 계산. 실제 EXIF Orientation=6 태그가 박힌 JPEG으로 재현/검증함:

```python
from PIL import Image
img = Image.new('RGB', (1000, 800), color=(255,255,255))
exif = img.getexif()
exif[274] = 6  # Orientation 태그
img.save('test.jpg', exif=exif, quality=90)
```

### 6. husky 훅 도입 직후 운영 서버 배포가 깨질 뻔함

**증상 (배포 전에 자체 발견)**: `/app/DEVELOPMENT.md` 컨벤션대로 husky pre-commit/pre-push
훅을 추가하고 `"prepare": "husky"`를 넣었는데, 운영 서버(140)의 배포 절차인
`npm install --omit=dev`가 `sh: 1: husky: not found` (exit 127)로 통째로 실패함.
**원인**: `prepare` 스크립트는 devDependencies 설치 여부와 무관하게 실행되는데, `husky`
자체가 devDependency라 `--omit=dev`로 설치하면 그 명령어가 없어서 실패함.
**조치**: `"prepare": "husky || true"`로 완화 — devDependencies가 있는 개발 환경에서는
정상적으로 훅이 걸리고, 없는 운영 환경에서는 조용히 넘어감. `rm -rf node_modules && npm
install --omit=dev`로 실제 재현 후 수정 확인함(회귀 체크리스트에 추가).

### 7. 여러 장 한꺼번에 업로드하면 초기 업로드 화면으로 되돌아감

**증상**: 사진 한 장은 정상인데, 여러 장을 한꺼번에 선택하면 편집 화면이 잠깐 떴다가 처음
업로드 화면으로 돌아감. 서버 로그엔 아무것도 안 남음(발송 전 단계라 요청 자체가 안 감).
**원인(강한 정황, 100% 확증은 아님)**: `handleFiles`가 `files.forEach(addPhotoCard)`로 선택된
사진 전부를 동시에 처리했다 — HEIC 변환(`heic2any`)과 Cropper 초기화가 사진 수만큼 동시에
돌아서, 사진이 여러 장(특히 아이폰 HEIC 고해상도)이면 모바일 브라우저가 메모리 부족으로
탭을 통째로 새로고침하는 것으로 추정. 새로고침되면 JS 상태가 다 날아가 "초기 화면으로
돌아간 것"처럼 보이고, 원인을 알려줄 콘솔 로그도 새로고침과 함께 사라져서 서버 쪽에서든
클라이언트 쪽에서든 로그로 확증할 방법이 없었음.
**조치**:

- `handleFiles`를 동시 처리에서 순차 처리(`for...of` + `await`)로 바꿔 피크 메모리를 줄임.
- 위 원인 추정이 맞는지 확인/모니터링할 수 있게, 업로드 시작 시 `sessionStorage`에 "처리
  중" 표시를 남기고 정상 종료 시 지운다. 이 표시가 남은 채로 페이지가 다시 열리면(=직전에
  업로드 도중 새로고침/크래시가 있었다는 뜻) 사용자에게 토스트로 알려준다 — 실제로 이
  증상이 재발하면 이 토스트가 뜨는지로 원인 추정이 맞았는지 검증 가능.

**추가 확증(2026-08-16)**: 사진 24장을 한꺼번에 선택하자 이번엔 "초기 화면으로 조용히
돌아가는" 정도가 아니라 크롬이 "이 페이지를 열 수 없음"(렌더러 크래시) 화면을 띄움 — 메모리
부족 추정이 사실상 확정됨. 순차 처리만으로는 막지 못하는데, 편집 화면에 동시에 떠 있는 카드
전부가 원본 해상도 이미지를 계속 메모리에 물고 있는 게 진짜 원인이라(순차 처리는 "로딩 중"
피크만 줄였을 뿐 "다 로딩된 뒤 동시에 유지되는" 메모리는 그대로였음). **1차 응급 조치**: 한
번에 편집 가능한 사진 수를 `MAX_PHOTOS_PER_BATCH`(10장)로 제한.

**근본 해결(2026-08-16, 승인 후 구현)**: 다운스케일 미리보기 아키텍처로 전환.

- 업로드 시 원본을 딱 한 번 디코드해서 `PREVIEW_MAX_DIM`(1280px) 이하로 축소한 미리보기만
  Cropper에 물린다. 디코드한 원본 픽셀 데이터는 다운스케일 직후 바로 버림 — 편집 화면에
  남는 건 미리보기 blob뿐. 원본은 인코딩된 상태(`entry.fullResBlob`, HEIC면 변환까지 끝낸
  상태)로만 가볍게 보관.
- Cropper의 캔버스 데이터는 "이미지 대비 비율"이라 해상도와 무관하다는 점을 이용해,
  `getCropRectFraction()`으로 미리보기 크로퍼의 크롭 위치/배율을 비율로 뽑아둔다.
- "전송" 시점에 사진을 하나씩 순서대로: 원본을 다시 디코드(`loadImageFromBlob`) →
  그 비율을 원본 해상도에 곱해 크롭(`cropImageByFraction`, 순수 canvas drawImage로 구현,
  Cropper.js를 다시 쓰지 않음) → 합성 → blob 변환 → 즉시 objectURL 해제. 동시에 메모리에
  남는 원본은 항상 최대 1장.
- 미리보기 crop.js를 안 쓰기로 하면서, EXIF 회전 문제(버그 #5)도 부수적으로 더 단순해짐 —
  `<img>` + `drawImage`는 브라우저가 이미 방향 보정한 픽셀을 그대로 그려주므로, 전송
  단계에서는 `effectiveNaturalSize`/`rotate` 신경 쓸 필요가 아예 없어짐(편집용 저해상도
  크로퍼 쪽만 여전히 씀 — 다운스케일 자체가 이미 방향 보정된 상태라 그쪽도 사실상 rotate=0).
- **검증 중 부수적으로 발견한 버그**: 자유 모드에서 사진을 극단적으로 축소(예: 5%)해두면,
  "이미지 대비 비율"로 역산한 크롭 영역이 원본보다 훨씬 커져서(배경이 많이 드러날수록
  크롭 영역이 상대적으로 커짐) 결과물이 비정상적으로 거대해짐(테스트 중 4574×7312=3300만
  픽셀 출력을 실제로 만들어냄). `MAX_OUTPUT_DIM`(6000px)으로 긴 변을 캡 씌워 해결 — 정상적인
  커버 핏(원본 해상도 이하)은 이 캡보다 한참 작아 전혀 영향받지 않음.
- 응급 조치로 걸어뒀던 장수 제한은 이제 크래시 방지 목적이 아니라(다운스케일로 이미 해결)
  DOM/이벤트 리스너가 무한정 쌓이는 걸 막는 안전판 정도라, `MAX_PHOTOS_PER_BATCH`를
  30장으로 올림.

Playwright로: (1) 2500×4000 원본 업로드 시 미리보기는 1280px 이하로 축소되지만 결과물은
2500×4000 그대로 나오는 것, (2) 인생네컷 자동감지+2단 배치+좌우반전이 큰 원본에서도 정확한
것(픽셀 검증), (3) 극단적 축소 시 `MAX_OUTPUT_DIM` 캡이 걸리는 것, (4) 실제 EXIF
Orientation=6 JPEG의 방향이 정확한 것, (5) 8장 배치가 순차로 전부 정상 처리되는 것까지 확인.

### 8. 실제로는 다 성공했는데 화면엔 "전송 실패(HTTP 504)"로 뜸

**증상 (실사용자 테스트로 발견)**: 사진 30장을 전송했더니 화면엔 "전송 실패: 전송 실패
(HTTP 504)"가 떴다. 서버 로그엔 에러가 전혀 없었고, Gmail 보낸메일함을 확인해보니 4통
(9+9+8+4=30장) 전부 정상 발송되어 있었다 — 실제로는 완전히 성공했는데 화면만 실패로 뜬
"거짓 실패".

**원인**: `/api/send`가 R2 백업 업로드 + 이메일 발송(사진이 많으면 여러 통으로 분할)까지
전부 끝난 뒤에야 응답하고 있었다. 사진이 많으면(30장, 이메일 4통) 이 전체 처리 시간이
Nginx Proxy Manager의 응답 대기 시간(기본값 60초 안팎)을 넘겨버려서, Nginx가 먼저 504를
브라우저에 돌려보냈다 — 그 시점에 우리 서버는 여전히 정상적으로 작업 중이었고 결국 끝까지
완료했다. 즉 타임아웃은 프록시와 브라우저 사이에서만 발생했고, 실제 작업 자체는 끝까지
성공했다.

**조치**: `/api/send`가 업로드를 받자마자(R2/이메일 작업을 시작하기 전에) 즉시
`{ok:true, accepted:N}`으로 응답하도록 바꿈. R2 백업 + 이메일 발송은 응답 이후
백그라운드에서 계속 진행하고, 결과(실패 포함)는 서버 로그로만 남긴다 — 처리 시간이
얼마가 걸리든 클라이언트 응답은 항상 업로드 접수 시점 기준으로 빠르게(수십 ms) 온다.
자격증명을 일부러 무효화한 상태로 재현 테스트해서, 응답이 즉시 오고 백그라운드 실패도
서버가 죽지 않고 로그로만 남는 것까지 확인함.

**트레이드오프**: 이제 R2 업로드나 이메일 발송이 (거짓이 아니라) 진짜로 실패해도 사용자
화면에는 알릴 방법이 없다 — 서버 로그로만 확인 가능. 가족 공유용 저트래픽 앱이라 감수하기로
함([[DECISIONS]] 참고).

### 9. 거울모드(나란히 2장 배치)에서만 얼굴이 세로로 길어짐 — iOS Safari의 drawImage 동작 차이

**증상**: 인생네컷을 "나란히 2장 배치"로 보내면 액자에서 얼굴이 세로로 늘어나 보임. 다른
사진들은 멀쩡한데 **거울모드일 때만** 그럼. 데스크톱 Chrome으로 아무리 테스트해도 재현이
안 돼서 한참 헤맸다.

**진단 과정** (이 순서가 중요 — 코드만 봐서는 절대 안 나옴):

1. 코드/수식상으로는 왜곡이 생길 수 없었다. `cropImageByFraction`은 `dw=round(sw),
dh=round(sh)`로 1:1로 그리고, 합성도 크기 변경 없이 붙이기만 한다. 실제로 Chrome에서
   Playwright로 정원을 넣고 3개 맞춤 모드 전부 돌려봤지만 전부 정상(800x798 등).
2. 그래서 **실제 전송된 결과물**을 R2에서 받아 픽셀로 쟀다(`kodak/202608170120_15-15.jpg`,
   3592x5749). 전체 비율은 0.6248로 정상이었지만, 사용자가 원본과 겹쳐본 이미지에서
   노란 패널 박스 비율이 1.21 → 1.03으로 달랐다.
3. "좌우가 잘린 것"인지 "가로로 압축된 것"인지가 갈림길이었다. 결과물에서 패널이 반쪽
   슬롯 폭의 몇 %를 차지하는지 쟀더니 0.846 — 잘림 가설의 예측값(0.975)이 아니라 압축
   가설의 예측값(0.833)에 맞았다. 즉 **스트립 전체 폭이 다 들어있는데 가로로 0.854배
   압축**돼 있었다(= 세로 17% 늘어남).
4. 압축 배율 0.854 = 1/1.1712인데, 1.1712는 그때 `frac.height` 값과 정확히 일치했다.
   여기서 원인이 잡혔다.

**원인**: `cropImageByFraction`이 소스 사각형을 이미지 밖으로 넘겨서 `drawImage`에 주고,
"넘친 부분은 알아서 잘리고 목적지도 같은 비율로 줄어든다"는 동작에 의존하고 있었다.

- Chrome: 스펙대로 소스를 자르고 **목적지도 같은 비율로** 줄여준다 → 여백 생기고 비율 유지.
- iOS Safari/WebKit: 소스만 자르고 **목적지는 그대로 둔다** → 이미지를 목적지에 꽉 채우도록
  늘려버린다.

거울모드만 그런 이유: 인생네컷은 자동감지로 "나란히 2장 배치 + 너비 맞춤"이 켜지면서
`frac.height`가 1을 넘는다(위아래 배경 여백). 일반 사진은 `frac`이 [0,1] 안에 있어 무관.

**조치**: 엔진 동작에 기대지 않고 소스 사각형을 이미지 경계로 직접 자르고, 목적지 사각형과
오프셋도 같은 비율로 우리가 계산해서 넘긴다. 이제 `drawImage`에는 항상 이미지 안쪽
사각형만 들어간다. 검증: Safari 동작을 재현하는 몽키패치를 깔고 옛 코드/새 코드를 같이
돌려서, 옛 코드는 정원이 800x935(세로 16.9% 늘어남), 새 코드는 800x799로 나오는 걸 확인.

**이어진 재설계**: 애초에 거울모드가 사진을 아주 좁은 비율로 "잘라서" 붙이는 구조라 이
경로를 밟았던 것이라, 사용자 요청으로 **자르지 않고 통째로 넣는 방식**(각 500×1600 슬롯에
contain + 좌우 4% 여백)으로 바꿨다([[DECISIONS]] 참고). 크롭이 없어져서 소스 사각형이
이미지 밖으로 나갈 일 자체가 사라졌다.

**교훈**: 브라우저 엔진의 "알아서 처리해주겠지" 동작에 의존하는 코드는, 개발 환경(Chrome)
에서 100% 정상이어도 실사용 환경(iOS Safari)에서 깨질 수 있다. 특히 실사용 리포트와
로컬 재현이 엇갈리면 **코드를 더 읽지 말고 실제 결과물 파일을 받아서 재는 것**이 빠르다.

## 버그 아니었던 것 (오인 리포트)

### "너비 맞춤일 때 상하 드래그가 안 됨"

실사용 리포트로 들어왔으나 조사 결과 버그가 아니었음. "너비 맞춤"은 이미지 폭을 크롭 박스
폭에 맞추는 것이라, 이미지가 프레임(또는 2단 배치의 절반 슬롯)보다 세로로 충분히 길지
않으면 애초에 드래그할 여지 자체가 없다(이미 세로 전체가 다 보이는 상태). 특히 2단 배치의
좁은 절반 슬롯에서는 일반적인 사진 비율로는 거의 항상 이 상황이 됨 — "너비 맞춤"은 프레임
자체보다 세로로 긴(좁고 긴) 사진에 쓸 때만 드래그 여지가 생긴다. 버그 #4(반복 토글 축소)와
동시에 리포트되어 처음엔 그 여파로 오인했으나, #4를 고친 뒤에도 독립적으로 재현/분석해서
정상 동작임을 확인함.
