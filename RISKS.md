# RISKS

아직 안 터졌지만 예상되는 문제, 그리고 의도적으로 미룬 개선사항. 실제로 터지면
[[ISSUES]]로 옮긴다.

## 미구현 기능 (의도적으로 범위 밖에 둔 것)

- **스포이드(자유 색상 추출)**: 배경색 후보를 가장자리 4곳 자동 추출로만 제공하고, 사진
  아무 곳이나 클릭해서 색을 뽑는 스포이드는 생략했다([[DECISIONS]] 참고). 가장자리 후보로
  부족하다는 피드백이 쌓이면 추가.
- **2단 배치 여백/거터 크기 조절 UI**: 현재 `OUTER_FRACTION`(2%)/`GUTTER_FRACTION`(4%)이
  코드 상수로 고정돼 있다. 사용자가 직접 슬라이더로 조절하게 해달라는 요청이 오면 UI를
  추가할 것 — 지금은 UI 복잡도를 낮추려고 일부러 뺐다.
- **파노라마(가로로 넓은 사진) 전용 대칭 배치**: "나란히 2장 배치"는 세로로 좁고 긴
  사진(인생네컷) 대응으로 설계됨. 가로로 넓은 사진을 위아래로 2단 복제하는 기능은 없다 —
  다만 배경 채우기(축소+배경색) 메커니즘 자체는 범용이라, 위아래 여백이 생기는 것 자체는
  이미 지원됨. "복제"가 필요한 경우만 미지원.
- **재업로드 자동화**: R2 백업은 순수 보관용이고, 액자 오류 시 사람이 R2 콘솔에서 직접
  다운받아 재업로드해야 한다([[DECISIONS]]). 이 과정 자체를 앱 안에서 자동화하지 않았다.

## 잠재 위험

- **액자 수신 확인 없음**: SMTP 발송이 성공했다는 것과 액자가 실제로 사진을 반영했다는 것은
  별개다. 이메일이 정상 발송돼도 액자 쪽 처리(용량 제한, 형식 거부 등)로 실제 업로드가
  실패할 수 있는데, 이 앱은 그걸 감지할 방법이 없다. 실제로 2026-08-16 30장 테스트 때
  Gmail 보낸메일함으로 "이메일 발송 성공"은 확인했지만, **액자 화면에 30장이 다 실제로
  뜨는지는 확인하지 않았다** — 아래 "이메일 한 통당 9장 제한" 항목과 겹치는 검증 공백.
- **이메일 한 통당 최대 첨부 장수(9장) 제한이 실제로 맞는지 미확정**: 사용자가 조사한
  자료(코닥 공식 1차 문서 아님, 조사 도구 요약)에 따르면 이메일 한 통에 10장 이상 첨부하면
  액자에서 일부 누락/지연될 수 있다고 한다. `MAIL_BATCH_MAX_COUNT`(9)로 반영해뒀지만
  ([[DECISIONS]] 참고), 이 숫자 자체가 실제 기기에 정확히 맞는지 실사용
  검증은 안 됨. 다음 실사용 테스트 때 **액자 화면에서 직접 장수를 세어 확인**할 것 —
  특히 지금까지의 테스트는 전부 "이메일이 발송됐다"까지만 확인했지 "액자에 몇 장이 실제로
  떴는지"는 확인한 적이 없다.
- **인증/rate limit 없음**: [[DECISIONS]]에서 의도적으로 뺀 부분이지만, 링크가 가족 범위
  밖으로 퍼지면 아무나 액자에 원치 않는 사진을 계속 올릴 수 있다.
- **모바일에서 고해상도 사진 다수 처리 시 메모리/성능**: 근본 원인([[ISSUES]] 버그 #7)은
  다운스케일 미리보기 아키텍처로 해결했다(같은 문서 참고). **실제 폰으로 30장 배치를
  편집→전송까지 검증 완료(2026-08-16)** — 크래시 없음. 다만 이 테스트 자체에서 별개의
  타임아웃 문제(버그 #8)가 드러났음(아래 항목).
- **R2/이메일 발송이 진짜로 실패해도 사용자가 알 수 없음**: [[ISSUES]] 버그 #8 조치로
  `/api/send`가 업로드 접수 즉시 응답하고 실제 R2/이메일 작업은 백그라운드에서 진행하게
  바꿨다. 거짓 실패(504)는 없어졌지만, 대신 **진짜 실패**(예: Gmail 앱 비밀번호 만료,
  R2 자격증명 만료)가 나도 사용자 화면에는 "성공"으로만 뜨고 아무도 모른다 — 서버 로그를
  직접 봐야만 알 수 있다. 가족 공유용 저트래픽 앱이라 지금은 감수하기로 했지만
  ([[DECISIONS]] 참고), 재검토 조건: 실패가 실제로 반복되면 실패 시 텔레그램/이메일
  알림 정도는 추가할 것(trend-cardnews의 승인 대기 알림 패턴 참고 가능).
- **`MAX_OUTPUT_DIM` 캡이 의도한 화질 저하를 유발할 가능성**: 자유 모드에서 사진을 아주 많이
  축소해둔 채로 보내면 결과물 긴 변이 6000px로 캡 씌워진다([[DECISIONS]] 참고). 정상적인
  커버 핏 사용에서는 전혀 영향 없지만, 극단적으로 축소한 경우 "화질보다 용량"을 택하는
  셈이라 나중에 컴플레인이 오면 캡 값을 조정하거나 정책을 재검토할 것.
- **HEIC 변환 실패 시 폴백 없음**: `heic2any`(클라이언트 사이드 변환)가 실패하면 그 사진은
  그냥 에러 처리되고 끝난다. 서버 사이드 변환 폴백이 없다.
- **서버 다운 모니터링 없음**: `kodak-frame-editor` systemd 서비스가 죽거나 140번 서버
  자체에 문제가 생겨도 알림이 오지 않는다. 사용하려고 들어갔을 때 안 되는 걸로 처음 알게 됨.
- **Cropper.js 내부 동작에 의존하는 우회 코드**: [[ISSUES]] 4번(setAspectRatio 축소 버그)과
  5번(EXIF 회전) 둘 다 Cropper.js 자체의 (문서화 안 된) 내부 동작을 관찰해서 우회한
  코드다. Cropper.js를 업데이트하면 이 우회 코드들의 전제가 깨질 수 있으니, 버전을 올릴
  때는 [[ISSUES]]의 회귀 체크리스트를 반드시 다시 돌려볼 것.

## 재검토 조건이 이미 걸려있는 항목들

아래는 [[DECISIONS]]에 이미 "이런 상황이 되면 재검토"라고 명시된 것들의 목록만 모아둠 —
자세한 내용은 DECISIONS.md 참고:

- 접근 제어(인증 없음) — 링크 유출/오남용 발생 시
- 크롭 해상도 정책(원본 유지) — 액자가 실제로는 스케일링을 잘 못한다고 확인되면
- R2 히스토리 UI 없음 — 재해복구 상황이 잦아지고 매번 찾기 번거로우면
